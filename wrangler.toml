name = "landing"
main = "src/index.ts"
compatibility_date = "2024-09-23"

# Static assets served from pages/ directory
# The ASSETS binding lets the Worker fetch assets programmatically (for A/B routing)
[assets]
directory = "./pages"
binding = "ASSETS"

# D1 database for signups, surveys, and analytics
[[d1_databases]]
binding = "DB"
database_name = "landing-db"
database_id = "placeholder-replace-after-d1-create"

# Uncomment after connecting unbound.tools domain to Cloudflare:
# [[routes]]
# pattern = "unbound.tools/*"
# zone_name = "unbound.tools"
